import * as React from '@material-ui/core/TableSortLabel/node_modules/react';
import { ExtendButtonBase } from '@material-ui/core/ButtonBase';
import { SvgIconProps } from '@material-ui/core/SvgIcon';
import { SimplifiedPropsOf } from '@material-ui/core/OverridableComponent';

declare const TableSortLabel: ExtendButtonBase<{
  props: {
    active?: boolean;
    direction?: 'asc' | 'desc';
    hideSortIcon?: boolean;
    IconComponent?: React.ComponentType<SvgIconProps>;
  };
  defaultComponent: 'span';
  classKey: TableSortLabelClassKey;
}>;

export type TableSortLabelClassKey =
  | 'root'
  | 'active'
  | 'icon'
  | 'iconDirectionDesc'
  | 'iconDirectionAsc';

export type TableSortLabelProps = SimplifiedPropsOf<typeof TableSortLabel>;

export default TableSortLabel;
